# Copy to .env and fill in from Neon Console → TaC-Dashboard → Connection details
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require"

# JWT-ondertekening voor sessies (genereer met: node -e "console.log(require(\047crypto\047).randomBytes(48).toString(\047base64url\047))")
AUTH_SECRET="vul-een-lange-willekeurige-string-in"
